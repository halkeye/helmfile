# Default values for nfs-secret.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

nameOverride: ""
fullnameOverride: ""

pv:
    server: "odin"
    share: 'foo'

pvc:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
