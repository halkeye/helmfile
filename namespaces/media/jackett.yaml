ingress:
  enabled: true
  hosts:
    - jackett.int.gfv.dev
  annotations:
    kubernetes.io/ingress.class: internal

persistence:
  enabled: true
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
