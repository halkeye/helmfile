ingress:
  enabled: true
  hosts:
    - jackett.int.g4v.dev
  annotations:
    kubernetes.io/ingress.class: internal

persistence:
  enabled: true
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
